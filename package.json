{
  "name": "scira-mcp",
  "version": "0.1.0",
  "description": "An XMCP application with tool syntax",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "xmcp build",
    "dev": "xmcp dev",
    "start": "node dist/http.js"
  },
  "dependencies": {
    "xmcp": "^0.5.6",
    "zod": "4"
  },
  "devDependencies": {
    "swc-loader": "^0.2.6"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@swc/core",
      "esbuild"
    ]
  }
}
